<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pd.it.menu.IMenuTreeDao">

	<select id="ra" resultType="VO">
		SELECT id as "id",
		parent_id as
		"parentId",
		parent_id as
		"pid",
		cn as "cn",
		en as "en",
		cn as "text",
		index_id as
		"indexId",
		url as
		"url",
		detail as
		"detail"
		FROM sys_Menu_t
		where
		<if test="id==null or id==''">parent_id is null or parent_id=''</if>
		<if test="id!=null and id!=''">parent_id =#{id}</if>
		order by
		index_id
	</select>

</mapper>  