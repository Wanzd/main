<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pd.it.app.job.IAppJobDao">
	<select id="ra" resultType="VO">
select t2.name "fromPerson",
	t3.name "toPerson",
	t1.rel_tag "relTag"
from app_person_rel_t t1
left join app_person_t t2
	on t1.from_id=t2.id
left join app_person_t t3
	on t1.to_id =t3.id
where t2.name like concat(#{name},'%')
	</select>
	
	<insert id="cs">
insert into app_job_t(
	job,
	company,
	location,
	salary,
	salary_start,
	salary_end,
	create_date,
	url,
	detail)
values
		<foreach collection="list" item="item" open="" separator="," close="">
(#{item.job},
	#{item.company},
	#{item.location},
	#{item.salary},
	#{item.salaryStart},
	#{item.salaryEnd},
	#{item.createDate},
	#{item.url},
	#{item.detail}
)
		</foreach>
	</insert>
	
	<update id="truncate">
truncate table app_job_t
	</update>
</mapper>  