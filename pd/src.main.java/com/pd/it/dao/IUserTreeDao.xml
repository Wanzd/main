<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pd.it.dao.User$IUserTreeDao">

	<select id="root" parameterType="VO" resultType="VO">
		SELECT
		id,target_id
		FROM User_rel_t
		where relation='儿子'
	</select>

</mapper>  