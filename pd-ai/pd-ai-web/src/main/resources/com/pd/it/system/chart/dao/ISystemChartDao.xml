<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pd.it.system.chart.dao.ISystemChartDao">
	<select id="query" resultType="com.pd.base.model.MapVO">
select id "id",
	datasource_id "dataSourceId",
	chart_type "chartType",
	title "title",
	x_title "xTitle",
	x_col "xCol",
	y_title "yTitle",
	y_col "yCol",
	z_title "zTitle",
	z_col "zCol"
from sys_chart_t
where id=#{fo.id,jdbcType=VARCHAR}
	</select>
	
	<select id="queryCombo" resultType="com.pd.base.model.MapVO">
select * from(
select distinct id "id",
	title "text"
from SYS_CHART_T
order by nlssort(title,'NLS_SORT=SCHINESE_PINYIN_M')
)
where 100>=rownum
	</select>
</mapper>  