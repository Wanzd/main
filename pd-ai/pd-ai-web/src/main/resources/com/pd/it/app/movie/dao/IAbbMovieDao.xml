<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pd.it.app.movie.dao.IAbbMovieDao">
	<update id="updateList">
begin
		<foreach item="it" collection="list" >
	insert into app_movie_t(
		source,
		id,
		name,
		url
	)
	values
	(
		#{it.source,jdbcType=VARCHAR},
		#{it.id,jdbcType=VARCHAR},
		#{it.name,jdbcType=VARCHAR},
		#{it.url,jdbcType=VARCHAR}
	);
		</foreach>
	commit;
end;
	</update>
	
</mapper>  